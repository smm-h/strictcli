# Version bump type: patch, minor, or major
bump = "patch"
# Short description of this release (required)
description = "Config show bool parity, negative values in config set, parseIntStrict wiring"
# Optional context explaining why these changes were made
context = "config show now outputs lowercase bools matching Python. config set accepts negative numeric values. Wired parseIntStrict to all int parsing sites. Error message change: unknown single-dash tokens now produce 'unexpected argument' instead of 'unknown flag'."
include = ["go"]
exclude = []
