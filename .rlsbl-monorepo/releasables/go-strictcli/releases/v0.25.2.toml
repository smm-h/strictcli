# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# Backfilled 2026-08-08: this release predates archive synthesis, so the file
# never existed. description/context/bump recovered from the GitHub Release
# notes for go-strictcli@v0.25.2; include/exclude mirror v0.30.0.toml (not recoverable).
# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["go"]
exclude = []
description = "Dict flag parse-error message now matches the sibling implementations"
context = "The dict flag missing-equals error now reads `--flag: expected key=value or JSON`, matching the Python and TypeScript implementations (Go has always accepted JSON object input for dict flags; the message understated it). Also, `config show --plain` now displays config-sourced values coerced to the flag's declared type, matching runtime resolution and the sibling implementations."
candidate_sha = "8f45b2fb4cb474672ae6d1a07b9504d5b163b966"

[tree_hashes]
go = "64e3d4d698538de550307bd6bf117dfd7d6f6b4f"
