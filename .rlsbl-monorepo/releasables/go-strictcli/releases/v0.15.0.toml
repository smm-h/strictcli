# strictspec document version gate (do not remove)
format_version = 1
candidate_sha = "5bb71098d4c84cb7cb7836029c228ef281c1a27a"
bump = "minor"
include = ["go"]
exclude = []
description = "Config set type coercion and key validation"

[tree_hashes]
go = "5a4fd92caebd9885fc82c9c87f58f11771dd8182"
# bump reconstructed by `rlsbl release backfill` from version arithmetic against 0.14.0
# include reconstructed by `rlsbl release backfill` from the targets detected at backfill time
# exclude reconstructed by `rlsbl release backfill` from the default: nothing excluded
# description reconstructed by `rlsbl release backfill` from changelog-md
