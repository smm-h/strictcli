# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["go"]
exclude = []
description = "Test coverage is anchored to a declared directory (test_coverage_dir, WithTestCoverageDir, testCoverageDir) instead of the current directory; the old boolean is refused. The package describes itself with the project's settled line."
candidate_sha = "f1132ff86417cc7489847dd52dd1b8f03c3b862e"

[tree_hashes]
go = "6fd9f7f2b8dfe68040896a26798f35d92e242c3d"
