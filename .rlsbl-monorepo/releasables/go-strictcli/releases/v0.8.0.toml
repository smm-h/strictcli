# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, or major
bump = "minor"
include = ["go"]
exclude = []
shipped_as = "go/v0.8.0"
# description reconstructed by `rlsbl release backfill` from github-release:go/v0.8.0
description = "**Breaking.** `WithChecksPath(path)` renamed to `WithChecks(path)`. CWD auto-discovery of checks.toml removed; checks must be explicitly enabled. checks.toml now requires a top-level `app` field matching the app name."
candidate_sha = "365830cd0032ca3ab1c847af3fa1a792d9fed665"

[tree_hashes]
go = "d0c48ccee5c48c14d03cb47a9baf18a62172c2a4"
