{"format_version":1,"id":"18cba4e0e250e4efcbdc801b499647e2848634440711fd43","commits":["b5ac9cf807d593ddaa5e8134c6e6eba2d9d239d1","ecbf3f6fec84d27a6376bb621778ab5991f9bec9"],"user_facing":true,"description":"**Presence is now declared, never inferred.** `FlagOpts` is a discriminated union on `presence`: every flag declares exactly one of `{ presence: \"required\" }`, `{ presence: \"optional\" }` or `{ presence: \"default\", default: <value> }`, and `ArgOpts` moves onto the same union with `required?: boolean` deleted. Declaring none or two is a registration-time error, `default: null` is refused with a redirect to `presence: \"optional\"`, a list or dict flag no longer defaults silently to `[]` / `{}`, a variadic arg cannot declare a default, and a mutex member declares its own optionality instead of receiving a parse-time exemption. An optional flag or arg delivers `undefined` (bools become a real tri-state), and the inferred handler-args type follows the declaration -- which fixes the mutex-member key that was typed as always-present while the parser handed it `undefined`.","type":"breaking","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cba4e25c69e9c721c1c42c2ff7481fa845933dc7855d51","commits":["b5ac9cf807d593ddaa5e8134c6e6eba2d9d239d1"],"user_facing":true,"description":"**`ctx.provided(name)`, and presence published everywhere it is asked about.** `ctx.provided` answers whether the invocation caused a flag's value -- true for `cli`, `env`, `config` and `implied`, false for `default` and `infra` -- so no handler reconstructs that boolean out of `ctx.source`. `--dump-schema` emits a `presence` key on every flag and arg entry plus a `default` exactly when presence is `\"default\"` (empty collections included), and the arg entry's `required` key is gone; help renders exactly one presence marker per line (`[required]`, `[optional]` or `[default: <value>]`), with required positionals and declared empty collections now visible; and MCP tool schemas read requiredness straight off the declaration, so a required bool finally appears in `required`.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cba4e266c343844198c08bd3a54915a1a4145c6e65548d","commits":["dd024d425d717fe08d6835bad62b73b332677784","698d625433dae6470c93dd1b78247713d2c6bd81"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cba4eb00b3d96015bebb97093741a0b8d00beb029c5855","commits":["798cd4af337e2496ba0e08166a68750495e54387"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cba5d447b0d60552076eedb8304652a411c798c02afd00","commits":["d9f9be37ea9f70835c336c624b33324e3e59d3da","15554bce48fa93a9bf528b06e012b21567caec45"],"user_facing":true,"description":"**A declared default never runs a flag's `validate` callback, and never counts as supplied.** A validator now sees only values the invocation caused -- CLI, env, config or an implication -- so a default the validator would reject no longer fails the parse when nothing is typed. The same one predicate decides presence for `allOrNone`, `requires` and `implies`, and it now excludes the `infra` source (a `relativeToRoot` default resolved through a declared root), which is the declaration deciding just as a plain default is.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cba5d5168da65b3b5a6ea087564b54baf56ae223e7f2e6","commits":["4fd020a365ffc407e62ecef8c76fd32801ebf2f2","c28124216dfede78c65a60580e9333e4530d2b24"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbb6c0e604d70cec0383b215af404c94f392562d2a4439","commits":["39e878820ffbf0dfd3693cf5e93c367e489c5f79"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbb76f0fd5e7afb3464a1bc6b14fdaa6a0612920652c6f","commits":["971e05dfba67f4df67a43fc3afbdeb571da84b76","f2215113c3a9dafc7cb06bfa06e37584c6109453"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbbc55583f70105be0045187eb44e9bf62ea121c8bacb0","commits":["5df0bb00af986070b37773ee32e1a41e920c312b"],"user_facing":true,"description":"**README explains the TypeScript-shaped declaration surface.** The package README now states that the TypeScript implementation is not a port of the Python or Go ones: flag and arg options are a discriminated union on `presence` (a `default` beside `presence: \"required\"` does not compile, and `presence: \"default\"` without a `default` does not either), the twin command factories narrow the handler's `ctx` so a `.write()` inside a read-only command is a compile error, and what the three implementations hold identical is behavior rather than spelling.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbca707c171c92553bf8a71d0e4891a7d6a3de13b4d6c9","commits":["a1ba2f226d8d9338b62f149081aed48370099a7d","80178b0817312c4abc91632480431daa6a3ab953","3ba51d8295a59ac422b74b57dd0193b50c9b36fd"],"user_facing":true,"description":"**Choices are a declaration scope.** `choiceFlag(name, choices, opts)` declares a flag whose type is its set of choices, each declared with `choice({ help, flags?, value? })` and owning the flags that exist only while it is elected. The handler receives one tagged value under the selector's own key -- a discriminated union derived from the literal choice map, so `switch (args.via.choice)` narrows and `assertNever` in the default branch is checked by the compiler, with no annotation written anywhere. Sub-flags are never top-level handler arguments at any depth, `provided(record, name)` answers provided-ness inside a scope, nesting is unlimited, and parsing is order-independent: `--subject hi --via email` parses exactly as `--via email --subject hi` does. A flag supplied outside its elected scope is its own parse error naming the flag, its owning choice and the choice that was elected -- never \"unknown flag\". A selector declares `required` or a `default` and never `optional`, a defaulted selection must be complete, and `choices` entries become `{ value, help? }` records with the bare-value spelling deleted -- `choices: [\"a\", \"b\"]` becomes `choices: [{ value: \"a\" }, { value: \"b\" }]`, and a flag whose entries carry help renders as an indented block instead of the one-line `[choices: a, b]` form.","type":"breaking","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbca7f09004e43bd767ae20b3f4d49a1cd716dacc917cf","commits":["f81b0241cf0a016aaa028d60b3e105105b7c017c"],"user_facing":true,"description":"**`mutexGroup` is removed.** Exactly-one-of-these is a member-spelled choice flag now: `memberChoiceFlag(name, choices, opts)` spells each choice as its own flag carrying its own payload, and it reproduces the mutex error sentences and the `--no-x` decline semantics byte-for-byte. There is no shim and no deprecation period -- replace `mutex: [mutexGroup({ a, b })]` with a `memberChoiceFlag` entry in the command's `flags` map. A member gains what a group could not express: a scope of its own, so `--profile work --create-missing` parses and `--all-profiles --create-missing` is a scope error, and a payload carried in the alternative that owns it, so the sentinel defaults on flags that meant nothing unless elected disappear. The handler receives one tagged record instead of one value per member, a member flag is `required once this member is elected` (inverting the rule that forbade requiredness on a member), and the `Flags (mutually exclusive):` help section is gone with the construct. Constraints now operate at root scope only: naming a scoped flag from `allOrNone`, `atLeastOne`, `requires` or `implies` is a registration error, because the scope already is the constraint.","type":"breaking","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbca80f1ba1fad0bf7b80c54d343bbb1279354aa69c395","commits":["4bae8afc756b92f4e71b7196059ba6002fc215c1","80178b0817312c4abc91632480431daa6a3ab953"],"user_facing":true,"description":"**The scope rule reaches the type system and the machine boundary.** A computed choice key is a compile error naming itself (it would silently degrade the delivered tag to `string` and make `assertNever` accept anything), a selector's `default` is typed `keyof C & string` so naming a choice that does not exist fails to compile before it fails to register, and a scoped sub-flag is unreachable as a top-level handler argument -- which is the structural fix for the handler-args unsoundness the mutex-member shape carried. Over MCP one command is still one tool: the selector contributes an `enum` property, every scoped flag contributes a top-level property that never appears in `required` (its requiredness is conditional), and the scope structure rides the tool description as a deterministic `Scoped parameters (enforced at call time):` block. A wrong combination is refused at call time with the same sentence the CLI parser gives.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbca8a01d3bca6ec3986d00de949d482805db10775cac0","commits":["da528e9c01519eef9eccd8468d2c0bfdfde7d860"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcceca533ee380c30d3655d904ab390c055fb5fa7212e","commits":["0e8a1f2694a5975d029c96152e4e5da2eb966d82"],"user_facing":true,"description":"**A structural parse problem is now reported ahead of a value that will not parse.** An unknown flag, an unknown choice and a scope violation are facts about the command line's shape, and shape is decided before any token's text is interpreted -- on every command, not only on one that declares a choice flag. `myapp cmd --count abc --unknown` reported `--count: expected integer, got 'abc'` and now reports `unknown flag '--unknown'`, and the answer no longer depends on which token came first.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcd28872a5f6c3ce58a4071da4b6e8073518d0d982472","commits":["ae7a46619a05a69f5cd6a5983d4f48754167be5b"],"user_facing":true,"description":"**An integer `choices` entry beyond \u00b12^53 is refused at registration.** The dumped schema publishes a declaration's choices as a JSON Schema `enum`, and a reader that parses JSON numbers as IEEE-754 doubles -- which every reader of a schema file is entitled to be -- reads a different integer back. Declare a big identifier as a string instead. Float choices are unaffected: the canonical float form round-trips exactly, so nothing is lost there.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcdd5178f8be03944e432fbd9474488ab539ccede0bd6","commits":["fea4fcb1cb4ef6d42fc1029e9433d6cecc9f49c3"],"user_facing":true,"description":"**`--dump-schema` writes schema version 2.** A flag or arg entry carries a real JSON Schema fragment under `value_schema`, from a closed subset of `type`, `items`, `additionalProperties` and `enum` with JSON Schema's own type names; the `type` key and the `repeatable` key are gone, because arity is a property of the value and a variadic arg publishes the same array fragment a `list[T]` flag does. A value flag's `choices` splits in two: the values as an `enum` inside the fragment, and the value-plus-help records beside it under `choices` (`help` omitted when an entry declares none). A choice flag has no fragment at all -- its value is a variant the subset cannot express -- and instead publishes its nested choices and scopes, each scoped entry a full flag entry, with `elect_by` as the discriminator. Every entity's keys are emitted in a declared order, the document is written in one canonical encoding so two implementations' dumps are byte-comparable, and the `defaults` block is rewritten as the complete map of what an omitted key means. The dump also stops being blind to behavior: `config_format`, `config_path`, `config_conflict_mode`, per-flag `prefixed` and per-command `flag_sets` each appear exactly when a declaration departs from the framework's own behavior, `config_path` publishes the declaration rather than the machine's resolution of it, and the `checks` block is a function of the declaration alone.","type":"breaking","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcdd6a3d921ade2dcceca195c4f3c9ff3283038e48270","commits":["fea4fcb1cb4ef6d42fc1029e9433d6cecc9f49c3"],"user_facing":true,"description":"**The MCP tool schema describes array parameters correctly.** A parameter whose value is a list carried its `enum` at the property root, which said the array itself had to equal one of the choices; it now sits inside `items`, describing an element. The projection derives its shape from the same arity rule the dumped `value_schema` fragment states, so a tool schema and a dumped schema cannot disagree.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcdd6ad3c1a5b0550752ced874c989e7f62854e30ab41","commits":["fea4fcb1cb4ef6d42fc1029e9433d6cecc9f49c3"],"user_facing":true,"description":"**A variadic positional arg accepts a list carrier.** `arg(\"files\", t.list(t.str), { variadic: true, ... })` registers beside the element-carrier spelling `arg(\"files\", t.str, { variadic: true, ... })`; both deliver the same array and publish the same schema fragment. The element spelling stays legal and stays the idiomatic one -- this widens the surface, it does not replace it. A list carrier on a non-variadic arg is still refused, and a dict carrier is refused on both.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcdd8254962f642dfd411036840ffa0242c407b9e1f67","commits":["ab85adae758b5e95cdf05f182e7963d6eabc2649"],"user_facing":true,"description":"**A positional arg with helped choices renders an indented block.** The block rule was always content-keyed -- any `choices` entry carrying help -- and it now reaches positional args exactly as it reaches flags: the entries render beneath the arg's line with their help, the one-line `[choices: a, b]` part is dropped in that form, and one alignment column is computed across the whole `Arguments:` section. A defaulted choice flag also renders its complete elected value (`[default: webhook (url=..., retries=5)]`) instead of the bare choice name, and a member-spelled choice flag's line carries its bare name with each member line ending in its own presence part.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcdd91125503a54137545335347be827f8250c149305d","commits":["55bc94db56159e727e423a5b220ec89d413cf064"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcde122cfd0c2dedd3538cec449959e001513e1fadca7","commits":["d4e5aa8a48e1f79f737d0b18ad523673a08efa4c"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbcdf0419ffd013b8da028d5b544e395d2296e3b204707","commits":["fc1242baad53b7848e84e927dc5927489fa838fa"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbdc63c0a17cf6e7877b459df9455e8239c9eed3e25f2b","commits":["e21804fb7b62ff82b857d6430d920446c14d14d5"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbdc6489694d581d8e4eb49a7e4e9a817c7c055109c81c","commits":["ddde9675253691472b6827a38654b34795647d74"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe06b27710dfcd451b8d703f8454aa2a9bbd21f1ecf38","commits":["703fa360e0b14896c7d4ec9bd7a29381eb0e8633"],"user_facing":true,"description":"**A member's payload property elects that member over MCP and `asTools`.** The flat machine form has no tokens, so supplying the property that flattens under a member's own flag name is that election: `{\"profile\": \"work\"}` now elects `--profile` and carries its value, exactly as `--profile work` does on the command line, without repeating the choice under the selector's own property. Supplying one member's payload beside a selector property naming a different member is a double election and is refused with the CLI's own sentence -- `--profile and --all-profiles are mutually exclusive` -- instead of a scope refusal that named the member's own election as the scope it was missing from. One level down the same rule holds: a payload key under an unelected outer scope names that outer scope, byte-for-byte as the parser does.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe06c742b45a1398720bd6a74471793b51f238cf065ce","commits":["72c7bf468e1c8c21b60c21130865603908889a22"],"user_facing":true,"description":"**A member name colliding with a command-level flag is reported as the duplicate flag name it is.** Electing a member IS the election, so a member's own name is a root token that opens no scope of its own. Declaring `memberChoiceFlag(\"mode\", { profile: ... })` beside a command flag named `profile` -- or two member-spelled choice flags that both declare a `profile` member -- now raises `command \"run\": duplicate flag name \"profile\"` instead of the simultaneously-electable message, which could only state the collision with two empty scope paths. A member may still carry its own selector's name, which is never typed under member spelling, and a nested member colliding with a command-level flag keeps the scoped diagnosis.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe06da37b21bc4e66c7ff7e9c40b8b755ad12e2e3f328","commits":["5b697c2558ac386669de470c2103a7af676028a2"],"user_facing":true,"description":"**A refusal inside nested scopes names the outermost election that came from outside the command line.** A required sub-flag missing under two nested choice flags blamed the innermost election, so a scope elected from an environment variable beside a typed inner one said nothing at all about the variable that caused it. The clause now walks the scope path outermost first and names the first election that did not come from the command line -- `(elected from env var 'MYAPP_VISIBILITY')` -- and stays absent when every election on the path was typed.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe06f1ae5196332966a1c14c34daaa455a09245227f2d","commits":["8a57b3bd915ce90366b4dc0e366c15814404ba20"],"user_facing":true,"description":"**A choice name must match `[a-z][a-z0-9-]*`, checked at registration.** A choice name is a surface name whatever elects it -- a token-spelled choice flag publishes it in an enum, a member-spelled one puts it on the command line as a flag -- and TypeScript spells it as a property key of the choice map, which constrains nothing. `choice({...})` under a key like `Email`, `e_mail` or `1email` is now refused with `Flag \"via\": choice name \"Email\" must match [a-z][a-z0-9-]*` in both spellings, before member spelling's flag-name bans, so a name that fails both is reported as the charset failure it is.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe06fcebed72a603f150ed23247668918c1ebb78a7c44","commits":["baee1764a66141922e7ec653280c7e752b02bf87"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe09ecda8afd0a2b71e2bb323406a9f5c62099f825ddb","commits":["fb725fcf4e4e971b3e8bdd8dbeb174289619dec1"],"user_facing":true,"description":"**The README documents the choice flag.** A new section shows `choiceFlag`/`memberChoiceFlag` and `choice`, the derived discriminated union a `switch` narrows with `assertNever`, the compile-checked `default` typed `keyof C & string`, and the fact that `optional` has no union member at all; the feature list gains a choice-flag entry and drops the retired mutex one.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe242603f6eaf9066cae49b834ea3b6c0579d062a6b9e","commits":["2be8daaf8d2367bbab9e786aabd2143ee7554f03"],"user_facing":true,"description":"**The flat machine form MCP and `asTools` take answers with the command line's own sentences.** Three refusals were wrong at that boundary. Elections ran one selector at a time, so an earlier selector's missing election was reported ahead of a later one's double election -- `{target: \"all-profiles\", profile: \"work\"}` on a command declaring a required `mode` first said `one of --fast, --slow is required` where the same command line says `--profile and --all-profiles are mutually exclusive`; every election now resolves command-wide, at every depth, before any scope, value or presence problem is reported. Electing a payload-carrying member through the selector's own key without its payload -- `{target: \"profile\"}` -- said `flag '--profile' is required under '--profile'`, naming the flag as its own owner, and is now `flag '--profile' requires a value`. And a payload-less member's own property was ignored, so `{all_profiles: true}` was refused with `one of --profile, --all-profiles is required` where `--all-profiles` elects; it now elects on `true` and declines on `false` exactly as `--no-all-profiles` does, decline clause and redundant-negation refusal included. A selector one level down also names the scope it sits in when it refuses, as it does on the command line.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe24b2c826795f85450881a5a44f7aeb158393f2818cb","commits":["c805aafc7849fed0b41ca49f53955d8b08e4cbeb"],"user_facing":true,"description":"**A record handed to `app.call()` electing a member with no `value` takes the same missing-value sentence.** `app.call(\"fetch\", { source: { choice: \"url\" } })` reported `flag '--url' is required under '--url'`; a member flag is elected by its own token and that token carries the payload, so the refusal is now `flag '--url' requires a value` at both front doors.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe24bef187492fa1ebbf7275a4f6a90ad016121c501b2","commits":["bfefd4c6307b0c393a4fdaed83d7ffaadf5b5892","08b2780f3a12adb03ba81b78891b5c844be5fbe4"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe4b214e43e12765016339b3749a18bdd9b98b5bdba62","commits":["b1310cb5ab496f4585dfa028738968c255f02c39"],"user_facing":true,"description":"**The machine boundary and `app.call()` answer with the command line's own sentences, in the command line's own order.** Four refusals were wrong. A key naming nothing the command declares was checked only after the selectors had been converted, so it lost to whatever that work found -- `{via: \"email\", nope: 1}` reported a missing election where the same command line reports `unknown flag '--nope'`; it is a fact about the object's shape and now outranks every election, scope, value and presence problem, at both doors, and a nested selector's own property is recognized so supplying one under a sibling scope is the out-of-scope refusal rather than an unknown-key one. `one of --profile, --all-profiles is required` put the decline note between the requirement and the scope it lives in and left out the clause naming the environment variable that elected that scope; the three clauses now compose as scope, then origin, then the decline note. A `relativeToRoot` default declared inside a choice's scope was delivered as the marker object itself where the same declaration at command level delivers the resolved path; it now resolves through the declared infrastructure root at every depth. And a pre-typed value is now checked against the type its declaration names -- at command level, inside a scope, on a member's payload and on an app-level global -- so `{count: \"7\"}` on an `int` flag is `--count: expected integer, got str` rather than a string handed to a handler, and `null` is a legal value for nothing: a flag that may be absent declares `presence: \"optional\"` and is delivered absent when the key is omitted.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe6c43c83e73634d2fb2070814d8eb38762c6b8936f79","commits":["0ba76627d696466eecfebf1baf05f7a20a946b18"],"user_facing":true,"description":"**`app.call()` checks a positional arg's value, and stages its phases over the whole command.** A positional was stringified on the way in, so `{ count: \"7\" }` reached an `int` arg as the integer 7 and `{ name: 123 }` reached a `str` arg as `\"123\"`; an arg is a declaration exactly as a flag is, so the supplied value is now checked against the type it declares -- `argument 'count': expected integer, got str` -- every element of a variadic arg is checked on its own, and `null` is a legal value for nothing. The record door also walked one selector to its end before starting the next, so every stage below election was decided for one selector before the next selector's record was read: a value, a presence or a scope problem in the first record was reported ahead of a second selector's election refusal. Elections, record shapes, scopes, values and presence are now staged across the whole command at every depth, the lowest stage decides what is reported, and the value sweep runs in declaration order rather than the order the caller happened to write the object's keys.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe6cb0e9077f78adfa3c02e7c49e0b925bc27945e1b77","commits":["9bea421fe13d0c5c338f5c93cec69945508c5c9d"],"user_facing":true,"description":"**A required bool inside a scope names the tokens that satisfy it.** `flag '--verify' is required under '--via email'` left a reader looking for a value to type, where the same declaration at command level says `flag '--verify' must be passed as --verify or --no-verify`. The scoped form is now the root sentence plus the scope suffix -- and the origin clause after it, when an ambient election put the requirement there -- on the command line and at both programmatic front doors alike.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe6cb18530abd5d9dc2bd3e89477ca5045cb848b3f662","commits":["8d5ff92e77721648484382c09ba2afea8ede87c1"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbe76908ffa8925604409b113143408c5a11a6477bb386","commits":["a099b3e7c68208487ed1242c61ad7e1e491a2770"],"user_facing":true,"description":"**A dash-spelled key is refused at `app.call()` too, and a wrong-typed value is heard before a missing election.** `call(\"run\", { \"dry-target\": \"x\" })` was accepted and delivered where the same key at the flat machine form is `unknown parameter \"dry-target\" for command \"run\"`: a flag's dashed spelling is its command-line token, and both doors take the underscored parameter name the schema publishes. The pre-typed value check also ran downstream of the flat form's conversion, so `{ via: \"email\", retries: \"nope\" }` on a command with a second required selector reported the missing election instead of `--retries: expected integer, got str`; the check now runs in the value phase at both doors, ahead of presence and behind every shape, election and scope fact.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbeb28690bc65fe55860bb4f9c4225bf81c13bed1cc106","commits":["dd2875fca71fc17431695d9e0226bd3aeee3e74e","4e25e1f3d2d0bcec56ca24a1bc38e2d64b16ed0b"],"user_facing":true,"description":"**A key an elected record's scope does not declare is refused, and a pre-typed positional binds to the arg its key names.** `call(\"run\", { via: { choice: \"email\", retries: 1, bogus: 2 } })` recorded a scope violation spelling the key as a flag -- `flag '--bogus' is only valid under '--via email'` -- for a key naming nothing the command declares anywhere. A record's key namespace is the elected choice's own scope, so the answer is now `unknown parameter \"bogus\" for command \"run\" under '--via email'`, at every depth and carrying the whole scope path, reported ahead of every election, scope, value and presence problem in the same call; a flag a sibling scope declares is an unknown key inside a record too, while the same parameter at the flat form's top level keeps the out-of-scope sentence it already had. A scoped refusal two levels down also named only its innermost election, where the command line names the whole path. And positionals handed to `call()` were read densely in declaration order: on a command declaring an optional `label` before a required `count`, `call(\"run\", { count: 2 })` bound 2 to `label` and answered `argument 'label': expected string, got int` -- a sentence about a key the caller never wrote. A value now binds to the arg its key names, and an omitted key is the absence presence answers.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbeb2989096fdcf399ca20bb204c2e8efdeec7ec7ae599","commits":["9444824181253700ff0d6b5d24d02525dd577cba"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbec833e6a15a9cba10fba1c2346d5ad2038eba8e45e22","commits":["8367d43b9a830ce639b205aa944ac098edae4fad"],"user_facing":true,"description":"**Two record-door refusals corrected.** An explicit `null` or `undefined` on an *optional* scoped field at `app.call()`'s record door now delivers absence -- a present field holding nothing -- instead of failing as a type error; that is how a caller writes an unset optional property, and required fields, defaulted fields and the flat door are all unchanged. And a `RelativeToRoot` naming an undeclared infra root inside a choice scope now says where it was declared (`... under '--via email' (elected by default)`) at every door instead of naming no scope at all.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbec85052e2b4e794d40d834684469940921d3a782caa4","commits":["8367d43b9a830ce639b205aa944ac098edae4fad"],"user_facing":true,"description":"**`provided()` over a caller-supplied elected record now answers `false` for every field.** The record door labels every field it delivers as the declaration's, so a handler can no longer tell a field the call supplied from one the declaration filled. The command line is unaffected and still answers what the invocation caused.","type":"breaking","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbeebc0a243685ee5543cd95404c09ab7d6af0e0cb4765","commits":["17cd807c60ac644e8d9105e4ed83c3abec06b8d7"],"user_facing":true,"description":"**The command line's value phase now reports in the order the tokens were typed.** `myapp run --via email --retries nope --before nope` named the root flag (`--before`) whatever the order, because every root flag's value was resolved before any scoped one; both refusals are true, and the one printed is now the token that comes first. A scoped `validate` callback can no longer refuse ahead of a later token's coercion failure either -- every coercion failure is reported before any callback refusal, whatever the declaration order. `call()` and the flat machine form are unchanged: they sweep the command's declarations once, as they always have.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbeebe2f1bff5e9db3e3f4733a4a24a11253a176f0fa72","commits":["811b5006442a5ef200b654dcf675849bfe6afa77"],"user_facing":true,"description":"**A selection nobody elected now reports the declaration at the flat machine door.** A command whose selector declares a default was delivered through `asTools()` / MCP with `ctx.source('via')` answering `cli` and `ctx.provided('via')` answering true, even when the caller sent nothing at all -- where the command line and `call()`'s record form both answer `default` and false for the same declaration. All three doors now agree. A scoped flag supplied beside a defaulted election is still delivered, exactly as the command line delivers it.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbeebe38e67e138fd7226db9ef4d52812c095f62546a1c","commits":["680c19b0292cd6578608ee9d4090b5544d2d8840"],"user_facing":true,"description":"**`config show` renders a `RelativeToRoot` default as the declaration.** The human table printed the marker object's internals -- its fields plus the source text of its `toString` -- instead of `db = RelativeToRoot('MYAPP_HOME', 'db.sqlite')  (source: default)`, which is what the other implementations print byte-for-byte. Under `--json` the payload carried the runtime's own casing; it now carries the machine-stable shape `--dump-schema` already uses, `{\"relative_to_root\": {\"env_var\": ..., \"parts\": [...]}}`. The resolved path is unchanged where it belongs: a run still delivers it, labelled `infra`.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbef34f80d1424f27f0ea3bea6494db209061eec8f76b6","commits":["4c354d5cb799acdb3ea6c344b34644326b125407"],"user_facing":true,"description":"**A refusal from a default-elected scope names what elected it at the flat machine door too.** A `RelativeToRoot` naming an undeclared infra root inside a selection nobody elected read `... under '--via email'` through `asTools()` / MCP, where the command line and `call()`'s record form both read `... under '--via email' (elected by default)` -- the clause exists to name the cause a reader cannot see in their own call, and a conversion is not a caller. Every scoped refusal at that door now carries it, at any depth, naming the outermost election no caller made. A caller's own election still earns no clause.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbf7e1937aaa175b2081669cf94f51812b6a822c0a0f97","commits":["e2b4bade4b52eb105c522b93803d568066ee88a7"],"user_facing":true,"description":"**A scoped value supplied at the flat machine door runs its `validate` callback.** A `validate` declared on a flag inside a choice's scope never ran for a value supplied through the flat machine form -- `tools/call` over `--mcp`, and a tool descriptor's `execute` -- so `{ via: \"email\", subject: \"hi\" }` was accepted where the same values typed as `--via email --subject hi` are refused with `--subject: hi is refused`. The callback is a property of a supplied value, and this door reads the caller's own keys, so it now runs for a scoped value exactly as it does for a root-scope one and for a typed token: at every depth, per element for a list, and never on a value the declaration decided. A coercion failure still outranks a validate refusal whatever the declaration order. `call()`'s record door is unchanged -- a constructed scope cannot tell a field the caller wrote from one it filled in, so it keeps the type check alone.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cbfa59a1f066607c69337df7c744638cd83e7b49d318ba","commits":["fa72054e017a92f59215f2e65dacd20ea020b311"],"user_facing":true,"description":"**A scoped value supplied at the flat machine door is checked against its `choices` list, and reports the call.** A closed set declared on a flag inside a choice's scope never constrained a value supplied through the flat machine form -- `tools/call` over `--mcp`, and a tool descriptor's `execute` -- so `{ via: \"email\", fmt: \"xml\" }` was accepted where `--via email --fmt xml` is refused with `--fmt: invalid value 'xml', must be one of: text, json`. A supplied scoped value now answers to the whole declaration there: the type, the closed set and the `validate` callback, with a coercion failure outranking both and an earlier field's callback outranking a later field's closed-set refusal. `provided()` over such a value now answers **true** with source `cli` too, as the command line answers for the same value -- that door reads the caller's own keys, so it no longer inherits the record door's uniform `default`. `call()`'s record door is unchanged.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc03d7e11cb8dab90b3661e6a84bfe8a4cb22011a405d3","commits":["1a3ba762fab0df7c70de5a94d7c961dc6628d587"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc03d7eeb5a181d9467677d6aa49879047702edb516c5d","commits":["ed758accb1d5d939b5eeddb078ed32be691c5fc7"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc03d7fc831289857deda5a0654280912f732ec095fcf0","commits":["fd07d3bc995d8675c9ae5225dfe0f4f22b3846c1"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc06e3cb551215ea415ab131b04d7e9914b12a1338ecd3","commits":["9f91cc1c121840c38ea37f89848aac330b24067f","36d827ceea8b3be05062b0b0df9a61176a637b85"],"user_facing":true,"description":"**Constraints, reshaped.** `coRequired` is deleted and `allOrNone` takes its place beside a new `atLeastOne`, both taking one option object with a mandatory `name` and passed through `constraints: [...]`. A member is a plain `{ name, when? }` record naming a flag, a positional arg or another named constraint, nested to unlimited depth; `members` is typed `[M, M, ...M[]]`, so a one-member constraint is a compile error, and `when` is the literal union `\"present\" | \"true\" | \"non_empty\"`, so a typo is one too. A bool member must declare its election, so `--no-x` can no longer engage a constraint while selecting nothing. `requires` and `implies` keep their semantics and take the same mandatory name. No member may declare `presence: \"required\"`. Constraints now render in `--help`, publish their members in `--dump-schema`, and project into MCP tool schemas as `anyOf` / `dependentRequired` with any remainder stated in the tool description.","type":"breaking","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc08c003300320e889fa143119412a834fc4ee8578b50d","commits":["62c8419591694d39080a35c308236d2973115161"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc08c0d99d2549cc93fb9e42b1416094d7d36666af6757","commits":["737bf31cf5d4216954706cb2f16ef5a6a2257a3b"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc08ec849aeb5304a24f4bce4a49b2a68c2d37e33fddb7","commits":["2b71ee68728d497b14fc65cedf1f9d4199921213"],"user_facing":true,"description":"**The README documents the constraint system.** A Constraints section covers the four factories and their mandatory names, the `{ name, when? }` member object, the `members` tuple and `when` literal union that make a one-member constraint and a selector typo compile errors, the mandatory bool election, nesting with the help block it renders, the required-member refusal, and the root-scope rule.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc15aac735cd970674cb43e52c461ab79a5f2ae70e6ace","commits":["a46de72e459df246942b79cf1a7d41df06605540"],"user_facing":true,"description":"**Registration reports the first error the contract pins when a constraint set has two faults.** The `Requires` / `Implies` operand guards ran early enough to outrank a required-member refusal, so an `Implies` whose trigger is not a bool was reported ahead of a co-occurrence member declaring `presence: \"required\"`. Those guards now run after every co-occurrence step, matching the Python and Go implementations.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc15ac2690bb1a8bf23f2f54d34e2690a90dd2ec649ea4","commits":["655f371010ace6e6920d5d67eb8f3c4c9c40c8c9"],"user_facing":true,"description":"**A constraint naming a member-spelled selector's election flag now says so.** `allOrNone`, `atLeastOne`, `requires` and `implies` reported `--all-profiles` as an unknown member or an unknown flag. The flag exists -- it is the token that opens its own scope -- so it now takes the scoped-operand refusal naming that scope, byte-identical to the sibling implementations.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc15ad0c921d8b4a5a1cad00ec4f3c84093d9411f3f3f8","commits":["7583a0d782a563ac1c1f3156e530939b19bb86da","b1ee7c8a2239e63e6c21a3c74ba87b0384323479"],"user_facing":false,"packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc17f35a62b96b848fbfd9369543079675247b4dad2a02","commits":["937648c48c8b6067436f9a209c6f159819547313"],"user_facing":true,"description":"**Registration reports the pinned first error when a constraint set has both a shape fault and an arity fault.** A bare-string member in a later constraint was reported only after an earlier constraint's \"must declare at least two members\" refusal. The member-record-shape check now runs across the whole set before any constraint's members are counted, so the bare name is named first -- matching the Python and Go implementations. Both faults are compile errors for a typed caller; the change is visible to a widened or JSON-shaped one.","type":"fix","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc253f7727d2320bc1cdf6aeaa437ca46901c7865f0e8e","commits":["4c4c62f5ca5cf62b197f0899d03b325071920033"],"user_facing":true,"description":"**Three breaking changes, all of them the update-command round's.** (1) The mutating-default ban: a flag or positional arg on a command declaring `effect: \"mutating\"` may no longer declare a value default, because on a mutating command a value the framework picked is a value the framework writes -- empty collection defaults, `relativeToRoot()` defaults, a choice flag's own default and every declaration on a read_only command stay legal. (2) `config set` takes its value under `--value`: its write is now an exactly-one selection over a value, a clear and a reset to default (`config set <key> --value <v>`), and supplying none or two of the three is refused by the framework rather than by the command. (3) The machine envelope gains a `writes` member carrying an update command's write set, and `interface_version` becomes `2`.","type":"breaking","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc2541046c25dacbb0bee9d43a4eb1a3bc21765688e414","commits":["4c4c62f5ca5cf62b197f0899d03b325071920033","90662a65dbf7b5475ed1125ee5a8b076dc31e8bf"],"user_facing":true,"description":"**The update-command construct.** `updateOf: { resource, writeMode, identity, properties }` on a command spec declares what a command updates: which flags and args name the instance, which flags carry what changes, and whether the write is sparse or a full replace. `properties` is typed over the command's own declared names with a floor of one, so a property naming a flag the command does not declare -- and an empty list -- are compile errors. The framework enforces that at least one property is supplied, renders the write set on every surface a run reports through (one unnumbered line in the would-do log and a `writes` member on the machine envelope), and publishes the declaration in `--dump-schema` and in the MCP tool schema. A property declares `presence: \"optional\"`: absence means untouched, and `nullable: true` mints `--unset-<prop>`, answered by `ctx.unset(name)`.","type":"feature","packages":["ts-strictcli"]}
{"format_version":1,"id":"18cc25431d57d3d81e1cafbe5a074c29b864cc611dc122e1","commits":["15958bc5ac5f6c3d46c088c2002f9df34ddd3657"],"user_facing":false,"packages":["ts-strictcli"]}
