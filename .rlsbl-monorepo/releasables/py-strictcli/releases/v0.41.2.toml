# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["pypi"]
exclude = []
description = "A plain CLI run no longer plants an empty .strictcli/coverage/ directory in the current directory, and the package describes itself with the project's canonical one-line description."
candidate_sha = "1e2f8a339aef87f87f91a82910549f2276be6ae3"

[tree_hashes]
python = "d71b2fac0f8c16386dc94c3b33d0ad913cca4cba"
