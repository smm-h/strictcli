# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor" # bump reconstructed by `rlsbl release backfill` from version arithmetic against 0.28.0
# Short description of this release (required)
description = "Breaking: ctx-first handler signatures, Outcome return type, required version, tomlkit config writes, float canon, notes channel, 7 parity fixes" # description reconstructed by `rlsbl release backfill` from github-release:py-strictcli@v0.29.0
# Optional context explaining why these changes were made
context = ""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["pypi", "npm"]
exclude = []
candidate_sha = "e24d1a858864b494bb9dc438053e3b2993937d4b"

[tree_hashes]
python = "8af2d13483a51e6b9c0b5ab519002b8017ad0fbe"
