# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["pypi", "npm"]
exclude = []
shipped_as = "strictcli@v0.10.0"
# description reconstructed by `rlsbl release backfill` from github-release:strictcli@v0.10.0
description = "**New feature.** `--dump-schema` omits fields matching defaults and includes a top-level `defaults` object documenting what missing fields mean."
candidate_sha = "acc268fa880abd843ee5e2819666834a6c493c9f"

[tree_hashes]
python = "d9b568a24eff012d024f6ab6313f30c292840e8a"
