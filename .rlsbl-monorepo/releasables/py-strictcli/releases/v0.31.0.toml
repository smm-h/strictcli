# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "npm wrapper discontinued and MCP error-message parity"
# Optional context explaining why these changes were made
context = """
The npm distribution of the Python implementation is discontinued. Starting at
0.31.0, the npm name is being reassigned to a native TypeScript implementation.
Python users should install strictcli from PyPI.
"""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["pypi"]
exclude = []
