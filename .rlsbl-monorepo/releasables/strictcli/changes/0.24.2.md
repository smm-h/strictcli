## 0.24.2

Fix publish router CI gate regex to match actual CI check run names.

<details>
<summary>Context</summary>

The publish gate used CI_CHECK_REGEX '^(strictcli) / ' but the CI Router produces check runs named 'strictcli-ci / ...' and 'strictcli-ci-pypi / ...'. The gate timed out waiting for non-existent check runs.

</details>

### Hotfix

- Fix publish router CI gate regex to match actual CI check run names.
