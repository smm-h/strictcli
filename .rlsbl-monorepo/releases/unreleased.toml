[packages.strictcli]
bump = "minor"
description = "Renamed Tag to FlagSet"
context = "Breaking: Tag class renamed to FlagSet, tags= parameter renamed to flag_sets= across all API surfaces. The Tag name was misleading — it implied labeling/annotation, but the type is actually a reusable bundle of flags."
include = ["pypi", "npm"]
exclude = []

[packages.go-strictcli]
bump = "minor"
description = "Renamed Tag to FlagSet"
context = "Breaking: Tag struct renamed to FlagSet, WithTags function renamed to WithFlagSets, Command.Tags field renamed to Command.FlagSets. The Tag name was misleading — it implied labeling/annotation, but the type is actually a reusable bundle of flags."
include = ["go"]
exclude = []

[packages.conformance]
bump = "minor"
description = "Updated for Tag to FlagSet rename"
context = "Schema, code generators, harness, API surface checker, and test cases updated to reflect the Tag→FlagSet rename in both implementations."
include = ["plain"]
exclude = []
