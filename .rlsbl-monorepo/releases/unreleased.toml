[releasables.py-strictcli]
# Version bump type: patch, minor, major, infra, or prerelease
bump = "patch"
# Short description of this release (required)
description = "Strict parsing: reject underscore separators in ints and overflow-to-infinity floats"
# Optional context explaining why these changes were made
context = ""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
include = ["pypi"]
exclude = []

[releasables.go-strictcli]
# Version bump type: patch, minor, major, infra, or prerelease
bump = "patch"
# Short description of this release (required)
description = "App-level help now lists global flags"
# Optional context explaining why these changes were made
context = ""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
include = ["go"]
exclude = []
