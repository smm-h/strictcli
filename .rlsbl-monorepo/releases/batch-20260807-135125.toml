[releasables.py-strictcli]
bump = "minor"
description = "Adds the `dry_run_supported=False` command declaration -- a mutating command can now refuse `--dry-run` with a mandatory reason carried into help and the schema -- and corrects a Python README that documented a handler signature, a check API and a dependency claim the framework no longer has."
context = '''
`--dry-run` is a promise: the preview must describe what the real run would do.
Some mutating commands cannot keep that promise, because their effects are not
representable ahead of time. Until now such a command either rendered a preview
that misrepresented it or hand-rolled a refusal inside the handler -- after
parsing, invisible to `--help` and to `--dump-schema`. `dry_run_supported` moves
the refusal to the registration site, where the framework can enforce it: the
reason is mandatory, it is rendered in a dedicated `Dry run:` help section, it is
emitted in the schema, and declaring it on a `read_only` command is a
registration-time error (a command that changes nothing has nothing to preview).

The same release completes a documentation truth pass. Every registration example
was re-checked against the shipped API, and the reserved flag quartet, the effects
regime and the `consequential` confirm protocol -- previously explained nowhere --
are now documented. Examples are no longer trusted by inspection: the
self-contained example programs are marked `validate`, and `selfdoc check` now
assembles and RUNS each one against the checkout, so documentation that stops
working fails the release rather than rotting quietly.
'''
include = ["pypi"]
exclude = []

[releasables.go-strictcli]
bump = "minor"
description = "Adds the `WithDryRunUnsupported(reason)` command option -- a mutating command can now refuse `--dry-run` with a mandatory reason carried into help and the schema -- and corrects a Go README whose examples panicked at registration."
context = '''
`--dry-run` is a promise: the preview must describe what the real run would do.
Some mutating commands cannot keep that promise, because their effects are not
representable ahead of time. Until now such a command either rendered a preview
that misrepresented it or hand-rolled a refusal inside the handler -- after
parsing, invisible to `--help` and to `--dump-schema`. `WithDryRunUnsupported`
moves the refusal to the registration site, where the framework can enforce it:
the reason is mandatory, it is rendered in a dedicated `Dry run:` help section, it
is emitted in the schema, and declaring it on a `read_only` command panics at
registration (a command that changes nothing has nothing to preview).

The same release completes a documentation truth pass. Every registration example
was re-checked against the shipped API -- several would have panicked on the
reserved flag names they used -- and the reserved flag quartet, the effects regime
and the `consequential` confirm protocol are now documented. Examples are no
longer trusted by inspection: the self-contained example programs are marked
`validate`, and `selfdoc check` now compiles and RUNS each one against the
checkout, so documentation that stops working fails the release.
'''
include = ["go"]
exclude = []

[releasables.ts-strictcli]
bump = "minor"
description = "Adds the `dryRunSupported: false` command declaration -- a mutating command can now refuse `--dry-run` with a mandatory reason carried into help and the schema -- generates the exported `VERSION` constant from package.json so it can no longer report a version the package never shipped, and rewrites an npm README usage example that did not compile."
context = '''
`--dry-run` is a promise: the preview must describe what the real run would do.
Some mutating commands cannot keep that promise, because their effects are not
representable ahead of time. Until now such a command either rendered a preview
that misrepresented it or hand-rolled a refusal inside the handler -- after
parsing, invisible to `--help` and to `--dump-schema`. `dryRunSupported` moves the
refusal to the registration site on both twin factories and both passthrough
twins, where the framework can enforce it: the reason is mandatory, it is rendered
in a dedicated `Dry run:` help section, it is emitted in the schema, and declaring
it on a `read_only` command throws at registration (a command that changes nothing
has nothing to preview).

`VERSION` was a hand-written literal with a test pinning that literal, so the test
protected the drift instead of catching it. It is now generated from package.json
by the prebuild hook that `prepack` inherits, and the test asserts agreement with
the manifest rather than a constant.

The same release completes a documentation truth pass. Every registration example
was re-checked against the shipped API, and the reserved flag quartet, the effects
regime and the `consequential` confirm protocol are now documented. Examples are
no longer trusted by inspection: the self-contained example programs are marked
`validate`, and `selfdoc check` now type-checks and RUNS each one against the
built `dist`, so documentation that stops working fails the release.
'''
include = ["npm"]
exclude = []
