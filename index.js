throw new Error(
  "excli is a Python library. Install it with: uv add excli (or pip install excli). " +
  "This npm package is a wrapper that installs the Python package. " +
  "Import it in Python: import excli"
);
