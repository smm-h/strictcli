throw new Error(
  "strictcli is a Python library. Install it with: uv add strictcli (or pip install strictcli). " +
  "This npm package is a wrapper that installs the Python package. " +
  "Import it in Python: import strictcli"
);
