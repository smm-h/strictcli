# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["plain"]
exclude = []
candidate_sha = "a1c4bdf5f70d6aab1b9c3cd6528f20185b1ed8cb"
description = "**New feature.** Conformance CLI tool with strictcli check system. Install with `pip install -e conformance/` and run `conformance check --tag pre-release` for all validation checks."

[tree_hashes]
conformance = "f6afa0bb3e80aab7ba7239d2778ce54a3f6dad6b"
# description reconstructed by `rlsbl release backfill` from github-release:conformance@v0.5.0
