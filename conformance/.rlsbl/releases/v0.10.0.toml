# strictspec document version gate (do not remove)
format_version = 1
candidate_sha = "1251abe1da87b7c19093e39c7131b26ca5f6db32"
bump = "minor"
include = ["plain"]
exclude = []
description = "Updated for Tag to FlagSet rename"

[tree_hashes]
conformance = "c12f6e7a5b8212fc81d3ac620c5b06fdbe833a03"
# bump reconstructed by `rlsbl release backfill` from version arithmetic against 0.9.0
# include reconstructed by `rlsbl release backfill` from the targets detected at backfill time
# exclude reconstructed by `rlsbl release backfill` from the default: nothing excluded
# description reconstructed by `rlsbl release backfill` from github-release:conformance@v0.10.0
